import styled from 'styled-components'

export const OrderContainer = styled.div`
  h2 {
    font-size: 16px;
    font-weight: 700;
  }

  p {
    font-size: 14px;
    font-weight: 400;
    line-height: 22px;
    margin-bottom: 24px;
  }
`
